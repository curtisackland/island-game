#include "PerlinNoise.hpp"

float PerlinNoise::dotProduct(vector2 const& left, vector2 const& right) {
    return left.x * right.x + left.y * right.y;
}

float PerlinNoise::perlin(float x, float y) {
    
    return 0;
}